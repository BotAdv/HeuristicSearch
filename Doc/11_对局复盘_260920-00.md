# 对局复盘 · 260920-00

> 本报告由 `experiments/analyze_game.py` 生成：用存档里的策略与参数**回放**整局，
> 打开策略的解释钩子逐轮导出决策依据，并对回放过程做一致性自检。

## 0. 对局概况

| 项 | 值 |
| --- | --- |
| 对局 ID | `cf2d34c94be5` |
| 标签 | 260920-00 |
| 模式 | human-feedback（秘密未知，反馈由人工录入） |
| 策略 | `two_phase` |
| 参数 | `{"count_policy": "auto_exact", "probe_order": "heuristic", "assume_distinct": true, "lock_known_positions": true}` |
| 结果 | **全部 CORRECT，已解出**，共 7 轮 |
| 一致性警告 | 0 条 |
| 最终支持集 | 属于 10 个 · 不属于 35 个 · 已定位 10 位 |

## 1. 回放校验：解释是不是“猜的”

| 种子 | 第1轮 | 第2轮 | 第3轮 | 第4轮 | 第5轮 | 第6轮 | 第7轮 | 完全复现 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `None` | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ | ✅ | 否 |
| `1` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 是 |
| `7` | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ | ✅ | 否 |
| `42` | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ | ✅ | 否 |

* **必然轮次**（对所有种子都复现）：第 1 轮、第 2 轮、第 3 轮、第 4 轮、第 5 轮、第 7 轮 —— 这些选择完全由规则约束唯一确定。
* **含随机平局的轮次**：第 6 轮 —— 候选里存在多个同样满足条件的组合，用随机数决定取哪个（不影响正确性，只影响步数）。

## 第 1 轮 · 阶段 A

* 支持集已确认 **0/10** 个成员，已排除 **0** 个，剩余 **45** 个未判定
* 阶段 A 已进行 1 轮；本轮从 **45 个未判定组合**中挑出 **10 个**做支持集分类：(1,1) (1,2) (1,3) (1,4) (1,6) (1,7) (1,8) (1,9) (1,10) (2,2)
* 挑选依据（未判定集合按“仍可能出现的位置数”降序、再按用过次数升序）：(1,1)（10 个位置可能 / 用过 0 次），(1,2)（10 个位置可能 / 用过 0 次），(1,3)（10 个位置可能 / 用过 0 次），(1,4)（10 个位置可能 / 用过 0 次），(1,6)（10 个位置可能 / 用过 0 次） …

**本轮猜测与每一位的依据**

| # | 组合 | 该位置候选集 | 选择依据 |
| --- | --- | --- | --- |
| 1 | (1,1) | 45 个 | 批次分类：(1, 1) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 2 | (1,2) | 45 个 | 批次分类：(1, 2) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 3 | (1,3) | 45 个 | 批次分类：(1, 3) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 4 | (1,4) | 45 个 | 批次分类：(1, 4) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 5 | (1,6) | 45 个 | 批次分类：(1, 6) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 6 | (1,7) | 45 个 | 批次分类：(1, 7) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 7 | (1,8) | 45 个 | 批次分类：(1, 8) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 8 | (1,9) | 45 个 | 批次分类：(1, 9) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 9 | (1,10) | 45 个 | 批次分类：(1, 10) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 10 | (2,2) | 45 个 | 批次分类：(2, 2) 还没判定过是否属于支持集，且它仍在本位置候选集内 |

回放结果：✅ 与存档一致。

**本轮反馈**：`MWMWWMPWMW`

判定明细：M×4，P×1，W×5 —— 由此新确认 **4** 个组合属于支持集，排除 **6** 个。

## 第 2 轮 · 阶段 A

* 支持集已确认 **4/10** 个成员，已排除 **6** 个，剩余 **35** 个未判定
* 阶段 A 已进行 2 轮；本轮从 **35 个未判定组合**中挑出 **10 个**做支持集分类：(3,8) (5,8) (7,8) (8,8) (3,3) (3,5) (3,10) (4,8) (5,5) (5,7)
* 挑选依据（未判定集合按“仍可能出现的位置数”降序、再按用过次数升序）：(3,8)（10 个位置可能 / 用过 0 次），(5,8)（10 个位置可能 / 用过 0 次），(7,8)（10 个位置可能 / 用过 0 次），(8,8)（10 个位置可能 / 用过 0 次），(3,3)（9 个位置可能 / 用过 0 次） …

**本轮猜测与每一位的依据**

| # | 组合 | 该位置候选集 | 选择依据 |
| --- | --- | --- | --- |
| 1 | (3,10) | 38 个 | 批次分类：(3, 10) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 2 | (5,8) | 28 个 | 批次分类：(5, 8) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 3 | (4,8) | 38 个 | 批次分类：(4, 8) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 4 | (7,8) | 28 个 | 批次分类：(7, 8) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 5 | (3,3) | 29 个 | 批次分类：(3, 3) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 6 | (5,5) | 38 个 | 批次分类：(5, 5) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 7 | (3,8) | 10 个 | 批次分类：(3, 8) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 8 | (8,8) | 28 个 | 批次分类：(8, 8) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 9 | (5,7) | 38 个 | 批次分类：(5, 7) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 10 | (3,5) | 32 个 | 批次分类：(3, 5) 还没判定过是否属于支持集，且它仍在本位置候选集内 |

回放结果：✅ 与存档一致。

**本轮反馈**：`MWWWPWPWWW`

判定明细：M×1，P×2，W×7 —— 由此新确认 **1** 个组合属于支持集，排除 **9** 个。

## 第 3 轮 · 阶段 A

* 支持集已确认 **5/10** 个成员，已排除 **15** 个，剩余 **25** 个未判定
* 阶段 A 已进行 3 轮；本轮从 **25 个未判定组合**中挑出 **10 个**做支持集分类：(6,6) (6,10) (10,10) (2,3) (3,6) (3,9) (6,9) (9,9) (2,6) (2,10)
* 挑选依据（未判定集合按“仍可能出现的位置数”降序、再按用过次数升序）：(6,6)（8 个位置可能 / 用过 0 次），(6,10)（8 个位置可能 / 用过 0 次），(10,10)（8 个位置可能 / 用过 0 次），(2,3)（7 个位置可能 / 用过 0 次），(3,6)（7 个位置可能 / 用过 0 次） …

**本轮猜测与每一位的依据**

| # | 组合 | 该位置候选集 | 选择依据 |
| --- | --- | --- | --- |
| 1 | (2,10) | 28 个 | 批次分类：(2, 10) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 2 | (3,6) | 16 个 | 批次分类：(3, 6) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 3 | (6,9) | 22 个 | 批次分类：(6, 9) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 4 | (6,10) | 15 个 | 批次分类：(6, 10) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 5 | (2,3) | 4 个：(2,3) (3,4) (3,9) (3,10) | 批次分类：(2, 3) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 6 | (9,9) | 25 个 | 批次分类：(9, 9) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 7 | (6,6) ⚠ | 2 个：(1,3) (2,8) | 批次分类：本位置候选集内已无剩余批次组合，改派批次里的 (6, 6) |
| 8 | (2,6) | 18 个 | 批次分类：(2, 6) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 9 | (3,9) | 19 个 | 批次分类：(3, 9) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 10 | (10,10) | 15 个 | 批次分类：(10, 10) 还没判定过是否属于支持集，且它仍在本位置候选集内 |

注：⚠ 标记的第 7 位，本轮填的是“批次里的待分类组合”，它不在该位置的候选集内，所以这一位本轮必然是 PARTIAL/WRONG，而不是 CORRECT——这是为了让该组合得到分类（阶段 A 的取舍：用部分的“白拿 CORRECT”换对一个组合的判定）。

回放结果：✅ 与存档一致。

**本轮反馈**：`PMWPPPWWWM`

判定明细：M×2，P×4，W×4 —— 由此新确认 **2** 个组合属于支持集，排除 **8** 个。

## 第 4 轮 · 阶段 A

* 支持集已确认 **7/10** 个成员，已排除 **23** 个，剩余 **15** 个未判定
* 阶段 A 已进行 4 轮；本轮从 **15 个未判定组合**中挑出 **10 个**做支持集分类：(4,10) (4,4) (5,10) (7,7) (2,8) (3,4) (4,7) (4,9) (7,9) (2,5)
* 挑选依据（未判定集合按“仍可能出现的位置数”降序、再按用过次数升序）：(4,10)（5 个位置可能 / 用过 0 次），(4,4)（4 个位置可能 / 用过 0 次），(5,10)（4 个位置可能 / 用过 0 次），(7,7)（4 个位置可能 / 用过 0 次），(2,8)（3 个位置可能 / 用过 0 次） …

**本轮猜测与每一位的依据**

| # | 组合 | 该位置候选集 | 选择依据 |
| --- | --- | --- | --- |
| 1 | (2,5) | 8 个 | 批次分类：(2, 5) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 2 | (4,7) | 10 个 | 批次分类：(4, 7) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 3 | (7,7) | 9 个 | 批次分类：(7, 7) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 4 | (5,10) | 4 个：(3,6) (3,10) (5,10) (10,10) | 批次分类：(5, 10) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 5 | (3,4) | 2 个：(3,4) (3,10) | 批次分类：(3, 4) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 6 | (4,9) | 3 个：(2,9) (4,9) (7,9) | 批次分类：(4, 9) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 7 | (2,8) | 2 个：(1,3) (2,8) | 批次分类：(2, 8) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 8 | (4,4) | 9 个 | 批次分类：(4, 4) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 9 | (4,10) | 5 个：(1,1) (2,8) (4,4) (4,10) (10,10) | 批次分类：(4, 10) 还没判定过是否属于支持集，且它仍在本位置候选集内 |
| 10 | (7,9) | 10 个 | 批次分类：(7, 9) 还没判定过是否属于支持集，且它仍在本位置候选集内 |

回放结果：✅ 与存档一致。

**本轮反馈**：`WWPWMPWPMW`

判定明细：M×2，P×3，W×5 —— 由此新确认 **2** 个组合属于支持集，排除 **8** 个。

## 第 5 轮 · 阶段 A

* 支持集已确认 **9/10** 个成员，已排除 **31** 个，剩余 **5** 个未判定
* 阶段 A 已进行 5 轮；本轮从 **5 个未判定组合**中挑出 **5 个**做支持集分类：(2,7) (2,9) (4,5) (5,9) (6,7)
* 挑选依据（未判定集合按“仍可能出现的位置数”降序、再按用过次数升序）：(2,7)（1 个位置可能 / 用过 0 次），(2,9)（1 个位置可能 / 用过 0 次），(4,5)（1 个位置可能 / 用过 0 次），(5,9)（0 个位置可能 / 用过 0 次），(6,7)（0 个位置可能 / 用过 0 次） …

**本轮猜测与每一位的依据**

| # | 组合 | 该位置候选集 | 选择依据 |
| --- | --- | --- | --- |
| 1 | (2,7) ⚠ | 2 个：(1,10) (4,10) | 批次分类：本位置候选集内已无剩余批次组合，改派批次里的 (2, 7) |
| 2 | (10,10) | 1 个：(10,10) | 已锁定：本位置候选集只剩 (10, 10) 这一个，继续沿用（白拿一个 CORRECT） |
| 3 | (4,5) ⚠ | 2 个：(1,7) (2,7) | 批次分类：本位置候选集内已无剩余批次组合，改派批次里的 (4, 5) |
| 4 | (3,6) | 1 个：(3,6) | 已锁定：本位置候选集只剩 (3, 6) 这一个，继续沿用（白拿一个 CORRECT） |
| 5 | (3,10) | 1 个：(3,10) | 已锁定：本位置候选集只剩 (3, 10) 这一个，继续沿用（白拿一个 CORRECT） |
| 6 | (2,9) | 1 个：(2,9) | 已锁定：本位置候选集只剩 (2, 9) 这一个，继续沿用（白拿一个 CORRECT） |
| 7 | (1,3) | 1 个：(1,3) | 已锁定：本位置候选集只剩 (1, 3) 这一个，继续沿用（白拿一个 CORRECT） |
| 8 | (6,7) ⚠ | 3 个：(3,4) (4,5) (4,10) | 批次分类：本位置候选集内已无剩余批次组合，改派批次里的 (6, 7) |
| 9 | (1,1) | 1 个：(1,1) | 已锁定：本位置候选集只剩 (1, 1) 这一个，继续沿用（白拿一个 CORRECT） |
| 10 | (5,9) ⚠ | 2 个：(1,10) (4,10) | 批次分类：本位置候选集内已无剩余批次组合，改派批次里的 (5, 9) |

注：⚠ 标记的第 1、3、8、10 位，本轮填的是“批次里的待分类组合”，它不在该位置的候选集内，所以这一位本轮必然是 PARTIAL/WRONG，而不是 CORRECT——这是为了让该组合得到分类（阶段 A 的取舍：用部分的“白拿 CORRECT”换对一个组合的判定）。

回放结果：✅ 与存档一致。

**本轮反馈**：`WCWCCCCWCW`

判定明细：C×6，W×4 —— 由此新确认 **6** 个组合属于支持集，排除 **4** 个。

## 第 6 轮 · 阶段 B

* 支持集已确认 **10/10** 个成员，已排除 **35** 个，剩余 **0** 个未判定
* 阶段 B（置换求解）：支持集已确定，只需把 10 个成员安置到 10 个位置。
* 各支持集成员尚未安置的副本数（本轮开始 → 本轮结束）：(1, 1)：0→0，(1, 3)：0→0，(1, 7)：1→1，(1, 10)：1→0，(2, 9)：0→0，(3, 4)：1→1，(3, 6)：0→0，(3, 10)：0→0，(4, 10)：1→0，(10, 10)：0→0
* 副本数 0 表示该组合已被 CORRECT 确认落位、没有余量；仍为 1 表示还没收到过 CORRECT 确认（可能已经由消去法锁定在某个位置）。

**本轮猜测与每一位的依据**

| # | 组合 | 该位置候选集 | 选择依据 |
| --- | --- | --- | --- |
| 1 | (1,10) | 2 个：(1,10) (4,10) | 指派：(1, 10) 已在支持集内且还有副本未安置，本位置候选集 2 个里优先安置它 |
| 2 | (10,10) | 1 个：(10,10) | 已锁定：本位置候选集只剩 (10, 10) 这一个，直接沿用 |
| 3 | (1,7) | 1 个：(1,7) | 已锁定：本位置候选集只剩 (1, 7) 这一个，直接沿用 |
| 4 | (3,6) | 1 个：(3,6) | 已锁定：本位置候选集只剩 (3, 6) 这一个，直接沿用 |
| 5 | (3,10) | 1 个：(3,10) | 已锁定：本位置候选集只剩 (3, 10) 这一个，直接沿用 |
| 6 | (2,9) | 1 个：(2,9) | 已锁定：本位置候选集只剩 (2, 9) 这一个，直接沿用 |
| 7 | (1,3) | 1 个：(1,3) | 已锁定：本位置候选集只剩 (1, 3) 这一个，直接沿用 |
| 8 | (4,10) | 2 个：(3,4) (4,10) | 指派：(4, 10) 已在支持集内且还有副本未安置，本位置候选集 2 个里优先安置它 |
| 9 | (1,1) | 1 个：(1,1) | 已锁定：本位置候选集只剩 (1, 1) 这一个，直接沿用 |
| 10 | (4,10) | 2 个：(1,10) (4,10) | 退化：候选集内已无“尚有未安置副本”的组合，只能取候选集里最少用过的 (4, 10) |

回放结果：✅ 与存档一致。

**本轮反馈**：`MCCCCCCMCM`

判定明细：C×7，M×3 —— 阶段 B 已没有未知组合需要分类；CORRECT 把该位置锁死，MISPLACED 说明该组合属于支持集但不在本位置，于是把它从本位置的候选中剔除。

## 第 7 轮 · 阶段 B

* 支持集已确认 **10/10** 个成员，已排除 **35** 个，剩余 **0** 个未判定
* 阶段 B（置换求解）：支持集已确定，只需把 10 个成员安置到 10 个位置。
* 各支持集成员尚未安置的副本数（本轮开始 → 本轮结束）：(1, 1)：0→0，(1, 3)：0→0，(1, 7)：0→0，(1, 10)：1→1，(2, 9)：0→0，(3, 4)：1→1，(3, 6)：0→0，(3, 10)：0→0，(4, 10)：1→1，(10, 10)：0→0
* 副本数 0 表示该组合已被 CORRECT 确认落位、没有余量；仍为 1 表示还没收到过 CORRECT 确认（可能已经由消去法锁定在某个位置）。

**本轮猜测与每一位的依据**

| # | 组合 | 该位置候选集 | 选择依据 |
| --- | --- | --- | --- |
| 1 | (4,10) | 1 个：(4,10) | 已锁定：本位置候选集只剩 (4, 10) 这一个，直接沿用 |
| 2 | (10,10) | 1 个：(10,10) | 已锁定：本位置候选集只剩 (10, 10) 这一个，直接沿用 |
| 3 | (1,7) | 1 个：(1,7) | 已锁定：本位置候选集只剩 (1, 7) 这一个，直接沿用 |
| 4 | (3,6) | 1 个：(3,6) | 已锁定：本位置候选集只剩 (3, 6) 这一个，直接沿用 |
| 5 | (3,10) | 1 个：(3,10) | 已锁定：本位置候选集只剩 (3, 10) 这一个，直接沿用 |
| 6 | (2,9) | 1 个：(2,9) | 已锁定：本位置候选集只剩 (2, 9) 这一个，直接沿用 |
| 7 | (1,3) | 1 个：(1,3) | 已锁定：本位置候选集只剩 (1, 3) 这一个，直接沿用 |
| 8 | (3,4) | 1 个：(3,4) | 已锁定：本位置候选集只剩 (3, 4) 这一个，直接沿用 |
| 9 | (1,1) | 1 个：(1,1) | 已锁定：本位置候选集只剩 (1, 1) 这一个，直接沿用 |
| 10 | (1,10) | 1 个：(1,10) | 已锁定：本位置候选集只剩 (1, 10) 这一个，直接沿用 |

回放结果：✅ 与存档一致。

**本轮反馈**：`CCCCCCCCCC`

判定明细：C×10 —— 阶段 B 已没有未知组合需要分类；CORRECT 把该位置锁死，MISPLACED 说明该组合属于支持集但不在本位置，于是把它从本位置的候选中剔除。

## 终局

全部 10 个位置均为 CORRECT，对局结束（7 轮）。
结束时支持集：支持集已确认 **10/10** 个成员，已排除 **35** 个，剩余 **0** 个未判定。

## 2. 各轮反馈对信念的实际收缩

| 轮次 | 反馈 | 候选集变化 |
| --- | --- | --- |
| 1 | `MWMWWMPWMW` | 候选集总规模 450 → 307（收缩 143）；位置变化：1:45→38，2:45→28，3:45→38，4:45→28，5:45→29，6:45→38，7:45→10，8:45→28，9:45→38，10:45→32 |
| 2 | `MWWWPWPWWW` | 候选集总规模 307 → 164（收缩 143）；位置变化：1:38→28，2:28→16，3:38→22，4:28→15，5:29→4，6:38→25，7:10→2，8:28→18，9:38→19，10:32→15 |
| 3 | `PMWPPPWWWM` | 候选集总规模 164 → 62（收缩 102）；位置变化：1:28→8，2:16→10，3:22→9，4:15→4，5:4→2，6:25→3，8:18→9，9:19→5，10:15→10 |
| 4 | `WWPWMPWPMW` | 候选集总规模 62 → 15（收缩 47）；位置变化：1:8→2，2:10→1，3:9→2，4:4→1，5:2→1，6:3→1，7:2→1，8:9→3，9:5→1，10:10→2 |
| 5 | `WCWCCCCWCW` | 候选集总规模 15 → 13（收缩 2）；位置变化：3:2→1，8:3→2 |
| 6 | `MCCCCCCMCM` | 候选集总规模 13 → 10（收缩 3）；位置变化：1:2→1，8:2→1，10:2→1 |
| 7 | `CCCCCCCCCC` | 候选集总规模 10 → 10（收缩 0）；各位置候选数不变 |

## 3. 决策构成统计

| 类型 | 位置次数 | 占比 |
| --- | --- | --- |
| 已锁定 | 23 | 32.9% |
| 批次分类 | 44 | 62.9% |
| 指派 | 2 | 2.9% |
| 退化 | 1 | 1.4% |
| **合计** | 70 | 100% |


## 一致性自检

✅ 全部通过：每一轮的反馈推理（CORRECT 锁定 / MISPLACED 排除 / PARTIAL 保留共享元素 / WRONG 保留不共享元素）都在候选集上正确生效，支持集归属也一致。
